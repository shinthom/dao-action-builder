{
  "name": "dao-action-builder-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "publish": "./scripts/publish.sh -t patch",
    "publish:patch": "./scripts/publish.sh -t patch",
    "publish:minor": "./scripts/publish.sh -t minor",
    "publish:major": "./scripts/publish.sh -t major",
    "publish:dry": "./scripts/publish.sh --dry-run"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
